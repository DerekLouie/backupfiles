Create a symlink to git version of vimrc (DO THIS IN HOME DIR):

ln -sf ~/backupfiles/.vimrc ~/.vimrc 
to stop: unlink ~/.vimrc

Install Vundle

Install powerline fonts (add to git repo actually)

for git: 
    git config --global core.editor vim 
create the vimhistory dir: ~/vimHistory

put this in bash_profile "source ~/backupfiles/Dotfiles/.bash_profile"

Intall tmux

link .screenrc

also .tmux.conf

brew install reattach-to-user-namespace


ln -sf ~/backupfiles/.vimrc ~/.vimrc 
ln -sf ~/backupfiles/.screenrc ~/.screenrc
ln -sf ~/backupfiles/.tmux.conf ~/.tmux.conf


brew list
ack				fontconfig			libgpg-error			libxslt				php54				unixodbc
apple-gcc42			fontforge			libksba				libyaml				pkg-config			vim
autoconf			freetype			libpng				mariadb				readline			zlib
automake			gettext				libtiff				markdown			reattach-to-user-namespace
bash-completion			jpeg				libtool				node				ruby-build
cmake				libevent			libxml2				openssl				tmux



=========== Bash Script ===========

* git pull vundle
* append line to bash_profile
* brew tmux
* brew install reattach-to-user-namespace
* brew install bash-completion
* brew install markdown
* brew install vim

ln -sf ~/backupfiles/.vimrc ~/.vimrc 
ln -sf ~/backupfiles/.screenrc ~/.screenrc
ln -sf ~/backupfiles/.tmux.conf ~/.tmux.conf
